import { DropUploadDirective } from './drop-upload.directive';

describe('DropUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DropUploadDirective();
    expect(directive).toBeTruthy();
  });
});
